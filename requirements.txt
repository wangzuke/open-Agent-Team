# open-teams runtime dependencies
# Python >= 3.10 required

# LLM providers (at least one is required)
anthropic>=0.40.0        # Anthropic Claude API
openai>=1.0.0            # OpenAI-compatible APIs (OpenAI, DeepSeek, Qwen, vLLM, etc.)
